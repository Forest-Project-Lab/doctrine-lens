# Doctrine Lens — DESIGN.md

この文書が、画面のあらゆる寸法・色・書体・動きの**正本**である。
ここに無い値を実装が持ってはならない。値を増やすときは、まずここを直す。

形式は [DESIGN.md](https://github.com/VoltAgent/awesome-design-md) の九節に従う。
規律は Linear と Raycast の設計体系から採った。**ただし値は採らない。**
この画面は VS Code の webview であり、主題（テーマ）は利用者が選ぶ。固定の hex を
持てば、明るい主題で読めなくなるか、編集器から浮くかのどちらかになる。
**採るのは規律、置くのは主題の変数である。**

---

## 1. 雰囲気と設計哲学 (Visual Theme & Atmosphere)

**この画面は帳票である。地図でも、ダッシュボードでも、図でもない。**

利用者が持ってくる問いはひとつだけ——「これを変えたら、何を、どの順で直すことに
なるか」。画面はその答えを、**上から下へ読む一本の明細**として返す。

したがって:

- **左寄せの行だけで構成する。** 中央揃えを使わない。座標を持たない。SVG を持たない。
- **視線は縦に一本。** 左端に記号の溝、そこから右へ題名、右端に補助の数。
  横に目を動かす必要があるのは一行の中だけで、行から行へは縦にしか動かない。
- **静かである。** 既定の状態で色が付いているものは、上流が「異常だ」と言ったものだけ。
  それ以外は無彩色である。装飾のための色・影・線を一つも持たない。
- **密である。** 狭い側帯（280px）で成立することを最初の受入条件とする。
  余白は読みやすさのために置くのであって、間を持たせるために置かない。

参照した規律の出所:

| 規律 | 出所 |
|---|---|
| 高さは影ではなく面の段で表す | Raycast「色の段から高さを生成」（影の使用を禁止） |
| 彩度のある色を chrome・釦・文字に使わない | Raycast「saturated accent は illustration 内だけ」 |
| 強調色は限られた役にだけ使う | Linear「lavender は brand mark・primary CTA・focus ring・link だけ」 |
| 暗い面に影を落とさない | Linear「Linear avoids drop shadows entirely on dark surfaces」 |
| 状態色は体系から降ってこない。自分で決めて明記する | Sentry の設計体系には error/warning の色が無い |

---

## 2. 色と役割 (Color Palette & Roles)

**固定の hex を持たない。すべて VS Code の主題の変数から取る。**

### 面の段（高さは色の段で表す。影は使わない）

| 役 | 変数 | 使う場所 |
|---|---|---|
| 地 | `--vscode-editor-background` | 画面全体 |
| 面 | `--vscode-editorWidget-background` | 起点の欄・要約の欄 |
| 触れている行 | `--vscode-list-hoverBackground` | 行の hover |
| 選んでいる行 | `--vscode-list-activeSelectionBackground` | 開いている行 |

### 罫

| 役 | 変数 | 使う場所 |
|---|---|---|
| 構造の線 | `--vscode-panel-border` | 段の区切り・起点欄の下辺 |

**装飾の線を持たない。** 罫は「ここで意味が変わる」ことを言うときだけ引く。
角丸は面にだけ当て、罫には当てない。

### 文字

| 役 | 変数 | 使う場所 |
|---|---|---|
| 主 | `--vscode-foreground` | 題名・記号 `!` |
| 従 | `--vscode-descriptionForeground` | id・パス・説明の一文・記号 `?` `~` |
| 連結 | `--vscode-textLink-foreground` | 押せる文字（コード範囲の行） |

### 釦

釦は一つだけである（「取り直す」）。主題の副次の釦をそのまま使い、独自の見た目を作らない。

| 役 | 変数 |
|---|---|
| 地 | `--vscode-button-secondaryBackground` |
| 文字 | `--vscode-button-secondaryForeground` |
| 触れているとき | `--vscode-button-secondaryHoverBackground` |
| 焦点の枠 | `--vscode-focusBorder` |

### 判定の色（**二色だけ**。上流が異常だと言ったものにだけ使う）

| 記号 | 変数 | 意味 | 出所 |
|---|---|---|---|
| `×` | `--vscode-charts-red` | 既に壊れている | `docs-audit` の severity=error/warn |
| `+` | `--vscode-charts-yellow` | 足りない | `dep-graph --reverse-orphans` |

**`!`（直す）と `~`（見直す）に色を使わない。** これらは異常ではなく、ただの波及集合である。
色を当てると「波及している＝悪い」という嘘をつく。

**禁止:**

- 型（SPEC・ADR・REQ…）を色で表さない。型は文字で読める。色は判定のためにある。
- ドメインを色で表さない。
- 三色目の彩度を導入しない。緑・青・紫・橙を画面に置かない。
- 彩度のある色を、背景・罫・釦・見出しに使わない。**記号の一文字にだけ載せる。**

---

## 3. 書体 (Typography Rules)

**四段だけ。** 段を増やすときはこの表を直す。

| 段 | 大きさ | 太さ | 行送り | 使う場所 |
|---|---|---|---|---|
| 起点 | 20px | 600 | 1.3 | 起点の文書の題名。画面で最も大きい文字 |
| 見出し | 15px | 600 | 1.4 | 波の見出し（「第 1 波 起点に直接ぶら下がる」） |
| 本文 | 13px | 400 | 1.6 | 各行の題名・説明の一文 |
| 補助 | 11px | 400 | 1.5 | 脚注・記号の語彙 |

**等幅は一段。** `--vscode-editor-font-family` を 12px で使う。

| 段 | 大きさ | 使う場所 |
|---|---|---|
| 等幅 | 12px | 記号・文書 id・`src/doctrine/cli.ts:1-231` のような位置 |

二段に分けていた（id を 11px、位置を 12px）。分ける理由が無かった。
比は 1.09 で、隣り合う段としては見分けが付かない。一段に畳んだ。

**比例の書体は `--vscode-font-family` を使う。** 主題が決めた字を上書きしない。

**太さは二種だけ。**

| 太さ | 役 |
|---|---|
| 400 | 読む（本文・説明・脚注） |
| 600 | 告げる（起点の題名・波の見出し・行の題名） |

`500` を使わない。中間の太さは、主題によっては 400 とも 600 とも区別が付かない。

**行送りは日本語を前提に 1.6 を既定とする。** 英語の設計体系は 1.5 を既定にするが、
この画面の本文は日本語であり、漢字とかなの混植では 1.5 は詰まる。

**禁止:**

- **隣り合う段の比を 1.15 未満にしない。** 段の差は見て分かる差でなければ、段ではない。
  この音階は `11 → 13 → 15 → 20` で、比は `1.18 / 1.15 / 1.33` である。
  旧実装は八段あったが、隣り合う比の中央値が `1.02` だった。段を名乗っていただけである。
- **段を五つ以上に増やさない。** 増やしたくなったら、その情報は本当に別の段かを疑う。
- 題名を等幅にしない。等幅は id と位置のためにある。
- 文字を切り詰めるより、行を折り返す。題名は情報であって装飾ではない。

---

## 4. 部品 (Component Stylings)

### 行（この画面の主役）

```
[ 溝 16px ][ 記号 12px ][ 8px ][ 題名 …………………… ][ 補助の数 ][ 溝 16px ]
                                説明の一文（従の色）
                                コード範囲（連結の色・等幅）
```

| 状態 | 見え方 |
|---|---|
| 既定 | 地。罫なし |
| 触れている | 背景を「触れている行」へ。**罫も影も足さない** |
| 開いている | 背景を「選んでいる行」へ。子の行が下に出る |
| 焦点（キーボード） | `outline: 1px solid var(--vscode-focusBorder); outline-offset: -1px` |

**記号は必ず左端の同じ位置に揃える。** これが走査性の源である。
文字を読む前に「後半は全部 `?` だ」が縦の縞として見える。

### 起点の欄

面の色。内側 16px、外側 16px、角丸 8。**罫を持たない。**
高さは面の段だけで表すので、面の上に線を重ねない（6 節）。
題名（起点の段）、その下に id・位置・状態・更新日を従の色で一行。

### 要約の一行

面の色。`10 文書を直す ・ コード 9 か所 ・ 直す場所が無い 6 件` の形。
**中黒で区切り、数を先に置く。** 数が無いものは出さない。

### 波の見出し

構造の線を上辺に引き、その下に見出しの段で一行。右端に件数。
**見出しは名前ではなく文である。**「第 2 波」ではなく「第 2 波　第 1 波が済むまで確定しない」。

### 脚注

補助の段。従の色。隠した件数・上流の名前と時刻・記号の語彙。

**この画面が持つ部品はこれで全部である。** 釦を本文に置かない。選択欄を本文に置かない。

操作子は画面の上端の帯にだけ置く。いま在るのは「取り直す」の一つと、
取得中を告げる語、そして「カーソルに従う」という一文である。
本文（明細）の中には操作子が一つも無い。

---

## 5. 配置 (Layout Principles)

**余白は 4px 格子。六段だけ。**

```
4 / 8 / 12 / 16 / 24 / 32
```

| 用途 | 値 |
|---|---|
| 記号と題名のあいだ | 8 |
| 行の上下 | 8 |
| 行の左右（溝） | 16 |
| 子の行の追い込み | 24（記号の溝ぶん右へ） |
| 段と段のあいだ | 16 |
| 起点の欄の内側 | 16 |

**角丸は二段だけ。** `4`（行の背景）と `8`（起点の欄）。

**縦一列である。** 段組みを持たない。幅が広がっても列を増やさない。
**幅が狭いほど正しく見える**のがこの配置の性質である。

---

## 6. 高さ (Depth & Elevation)

**影を使わない。** `box-shadow` `text-shadow` `filter: blur` `gradient` を一つも書かない。

高さは面の段だけで表す。

```
地（editor-background）
  └ 面（editorWidget-background）        起点の欄・要約
  └ 触れている行（list-hoverBackground）
  └ 選んでいる行（list-activeSelectionBackground）
```

段を飛ばさない。三段より深くしない。

---

## 7. すること・しないこと (Do's and Don'ts)

### すること

- **記号を左端に揃え、縦の縞を作る。** 形が答えを運ぶ。
- **上流が書いた日本語を一字も削らずに出す。** 上流の所見の文は「循環を断つこと」
  「後継へ張り替えるか印を消す」と直し方まで書いた完成した文である。書き直さない。
- **畳んだら必ず件数を書く。**「起点に繋がらない 28 文書は出していない」。
  隠すことは抽象ではない（Hickey）。
- **良い状態を空白で表さない。** 「壊れている 0 ・ 足りない 0 ・ 循環 0」と数で言う。
- **明細は必ず `file:line` で終わる。** 「要求から実装まで辿る」がそこで着地する。
- **時刻は `2026-07-29 09:14` の形で出す。** 年から分へ、大きい順。秒を出さない。
  地域ごとの並べ替えをしない。判定の新しさが読めればよく、`7/29/26` と `29/7/26` の
  どちらかで迷う余地を残さないためである。

### しないこと

- **図を描かない。** 節点も辺も座標も持たない。
- **型を色で表さない。**
- **操作子を本文に置かない。** `<select>` を一つも置かない。
- **恒久的な操作説明を出さない。**「Double-click to go deeper」のような文を常設しない。
- **段を移る仕掛けを作らない。** この画面に深度は無い。
- **選択肢を、独立して見せるために足さない。**
  （旧実装は「配置をダイヤルとして成り立たせるため」に意味のない選択肢を足していた）
- **切り詰めた文字列を主文にしない。**

---

## 8. 幅への応じ方 (Responsive Behavior)

**基準は 280px。** VS Code の側帯の最小幅である。ここで成立しなければ設計が誤っている。

| 幅 | 振る舞い |
|---|---|
| 280px | 既定。題名は折り返す。題名の行に収まらない補助の数は次の行へ回る |
| 600px 以上 | 変わらない。**列を増やさない。** 行の長さは読み幅 `640px` で止める |

**幅ごとの分岐（`@media`）を一つも持たない。** 折り返しは `flex-wrap` に任せる。
分岐を書くと、その境目の前後で別の設計を保つことになり、片方だけが古びる。

**読み幅は `640px` の一つだけ。** 本文 13px でおよそ 72 字にあたる。

`ch` で書かない。`ch` はその要素自身の字の大きさを基準にするので、
11px の脚注は 500px、13px の波は 600px、20px の起点は 900px で切れる。
節ごとに右端がずれ、区切りの罫の長さが揃わない。実際にそうなった。
**揃うべきものを揃えるために、単位は一つに固定する。**

**横に巻かない。** `overflow-x` を持たない。横に溢れるものを置かない。
**縦にだけ巻く。**

触れる的の高さは 24px 以上（行の上下 8 + 本文 13×1.6 ≒ 29px）。

---

## 9. エージェントへの指示 (Agent Prompt Guide)

この画面を触るとき、次を守ること。

1. **新しい値を思いつきで置かない。** 余白は 4/8/12/16/24/32 の六つ、角丸は 4/8 の二つ、
   書体は四段、等幅は一段、太さは二種、読み幅は一つ。
   それ以外の値を書いたら、それは誤りである。
2. **色を足したくなったら、まず「上流がこれを異常だと言っているか」を問う。**
   言っていないなら無彩色にする。
3. **`<svg>` を書きたくなったら、その情報が文で言えないかを先に問う。**
   ほとんど言える。循環でさえ `A → B → A` の一行で言える。
4. **説明を足したくなったら、まず消すものを探す。**
   Linear の 2025 年の改訂は引き算の集合だった。
5. **この文書と実装が食い違ったら、実装を直す。** この文書が正本である。
   意図して変えるなら、この文書を先に直し、統治木の ADR で理由を残す。

---

## 出所

- DESIGN.md 形式 — https://github.com/VoltAgent/awesome-design-md
- Linear の設計体系 — 同リポジトリ `design-md/linear.app/DESIGN.md`
- Raycast の設計体系 — 同リポジトリ `design-md/raycast/DESIGN.md`
- Sentry の設計体系 — 同リポジトリ `design-md/sentry/DESIGN.md`
  （状態色を持たないことの確認に使った）
- VS Code webview の指針 — https://code.visualstudio.com/api/ux-guidelines/webviews
- 何を出すかの決定 — `doctrine_docs/lens/decisions/` の ADR
