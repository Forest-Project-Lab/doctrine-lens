# 変更の記録

書式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に従い、
版の付け方は [Semantic Versioning](https://semver.org/lang/ja/) に従う。

「なぜ決めたか」は統治木の ADR が持つ。ここが持つのは「何が変わったか」だけである。

## [0.8.0] — 2026-08-02

**「現行だ」と言うのをやめた**（CHANGE-010・ADR-021）。

上流 0.8.0 が節点の白名簿を捨てて項が 8 から 14 に増えたので、何を足せるかを見にいった。
**測ったら、足すものは無く、消すものが在った。**

### 消したもの

- **行の `current`。** 実測（`497e13b` の木・70 文書）で、全節点を順に起点に置いて出た **273 行のうち 225 行（82.4%）**が
  `current` を表示していた。**四行並べて四行とも同じ語**で、何も区別していなかった。
  `SPEC-006` の受入は最初から「**非現行の**行に status が出て」と書いており、実装が言い過ぎていた。
- **`projectionTypes` の取得。** 地図を捨てた `CHANGE-004` 以来、実装のどこからも
  使われていなかった。**取って捨てていた。**
- **写しの道具が持っていた登録簿の問い合わせ。** 本体と同じ取得を使う形にした。

### 足したもの

- **要約の「非現行 N」。** 行から語を消すのは「良い状態を空白で表さない」（`ADR-014`）と
  正面からぶつかる。**消した分を数で支える。** 読み手は N と、status を持つ行の本数を
  突き合わせられる。実測 17 行の画面で **4 / 4** が一致する。
- **登録簿が取れない回の道。** 現行の集合が分からなければ**隠さない**——全行に status を出し、
  数は出さない。「取れなかった」を「全部現行だった」に化けさせない（`ADR-017`）。

### 足そうとして、実測で却下したもの

| 案 | 実測 | 判断 |
|---|---|---|
| 行に型を語で出す | `type` が id の接頭辞と **70/70 で一致** | 情報が一つも増えない |
| ドメインを越える行に印 | 全 273 行で越えは **0 件** | 自分の木で一行も効かない |
| `review_by` を副文へ | 持つ節点は **70 件中 1 件** | 66 行に何も出ない |
| `owner` を上流へ頼む | この木では **67/70 が同じ値** | 誰も区別しない |

### 直したもの

- **`_system/glossary.md` の `owner`。** 雛形が置いた `<記入>` が、
  **用語辞書の正本に初期化から今日まで残っていた。** その間に ADR を 20 本書き、
  監査は一貫して error 0 だった。上流へ報告した
  （[#182](https://github.com/Forest-Project-Lab/doctrine/issues/182)）——
  雛形の山括弧は **36 検査すべてを通り抜ける**。

### 門

- 単体 **212 件**（206 → 212）。突然変異の表 **55 行**（50 → 55、55/55 捕獲・生き残り 0）。
  統合 10/10。写し 誤り 0・食い違い 0。監査 error 0・warn 0。
- 写しの門に「語る行の本数と要約の数が一致すること」を足した。
  故意にずらして両方向で赤くなることを確かめた。

### 門そのものが四つ壊れていた

**この一周でいちばん重い発見である。** 門が全部緑になったあと、
`ADR-017` に従って**門とは独立に走査**をかけた（四観点・各指摘を三視点で反証）。
36 件の疑いのうち 25 件が反証で落ち、11 件が残った。

| 何 | 何が起きていたか |
|---|---|
| 写しの門 `statusAgrees` | 既定の起点では非現行も status 欄も 0 なので毎回 `0 === 0` で通る。**行の status 欄を丸ごと消しても緑だった** |
| 孤児の門 `006-26` | 注釈を落とさずに数えるので、**この試験自身の注釈**が三つの項を名指しているせいで、実装が一つも読んでいなくても通る |
| 表を読む門 | `from: (.+)` が改行を跨がず、**55 行のうち 2 行を黙って読み飛ばしていた。** 単引用符の行も両方の数え方から同時に落ちる |
| 潰しの走らせ方 | commit から隔離木を立てるので、**未コミットで回すと一つ前の版を検める。** 20 分気づかなかった |

四つとも塞ぎ、故意に壊して両方向で赤くなることを確かめた。
**門が発火していないことを、門が自分で言う**形にした（`statusAgrees` は
両方零のとき「不変条件を一度も試していない」として落ちる）。

### 測った数に刻印を添えた

この木は統治文書を足すたびに育つ。**この一周の中だけで節点は 67 → 70 → 72 と動き、**
文書五つに散った 34 箇所の数が実測と合わなくなっていた。

数に刻印を添える形へ書き直し（「`497e13b` の木・70 文書で測ると」）、
**手で数えるのをやめた**——`tools/measure-tree.mjs` が刷る（`ADR-021` 決定 5）。

### 積んだもの

走査が挙げた残りを、主題ごとに切り出した。

| | 何 |
|---|---|
| `CHANGE-011` | 受入の番号が、仕様と試験でずれている |
| `CHANGE-012` | 「取れなかった」を「無い」と言っている箇所が、まだ六つ在る |
| `CHANGE-013` | 発火していない門が、まだ三つ在る |

## [0.7.0] — 2026-08-02

**上流が答えたので、継ぎを捨てた**（CHANGE-009・ADR-020）。

doctrine 0.8.0 が、こちらから出した三本に答えた。

| # | 上流が何をしたか | 実測 |
|---|---|---|
| [#149](https://github.com/Forest-Project-Lab/doctrine/issues/149) | **節点の白名簿そのものを捨てた。** 組み立てが入れた項をすべて返す | 8 項 → **14 項**。`title` が載る |
| [#150](https://github.com/Forest-Project-Lab/doctrine/issues/150) | `trace-index.py` が git に訊く | 範囲 30 → **23 件**、`.gitignore` 配下が **0** |
| [#151](https://github.com/Forest-Project-Lab/doctrine/issues/151) | 辺に `mirrored`（両端書きの印） | 辺 104 本のうち **20 本** |

こちらは「`title` を足してほしい」と頼んだが、**上流はより深い原因を直した。**

> 以前は八項の白名簿で絞っており、**正本がどこにも無いまま組み立てと別々に手で保つ形**に
> なっていた。そして実際にずれた——**必須項の題名は最初から集められてさえいなかった。**

### 捨てたもの

- **`src/doctrine/titles.ts`（137 行）。** 上流の内部モジュール `_frontmatter` を
  `python3 -c` で直接呼んで題名を補う継ぎだった。**統治木を二度歩いていた**（実測 324ms）。
  `ADR-018` が「届いたらこの層は捨てる」と予告していた。
- **`trace_exempt` の `out/` と `dist/`。** `.gitignore` と同じ内容を二重に持っていた。
- **畳み方の判断。** `mirrored` を読み、根拠を上流へ返した。
  どちらを理由として見せるか（依存を残す）だけが画面の判断である。

### 実測

| 何 | 前 | 後 |
|---|---|---|
| 統治木を歩く回数 | 2 回 | **1 回** |
| 部分失敗の項 | 6 つ（`titles` を含む） | **5 つ** |
| 取得の総時間 | — | 1449ms（節点 65・題名 65・範囲 22・承認語 33） |
| 実装の行数 | — | **差し引き −114 行** |

### 決めたこと（ADR-020）

- **上流が答えたら、その回で継ぎを捨てる。** 「あとで」に送らない。
- **継ぎを置くときは、捨てる条件を同じ文書に書く。書かない継ぎは置かない。**
- **上流が返した判断を、こちらで作り直さない。** 事実は上流、見せ方はこちら。

## [0.6.0] — 2026-08-02

**数を出したら行き先も出す。行き先が無いなら、無いと言う**（CHANGE-008・ADR-019）。

脚注は「この画面のどこにも出していない所見が 4 件」と数だけを出していた。
数は書いたが、読み手は次に何もできない。

### 実測で分かったこと

この数は**性質の違う二つを畳んでいた。**

```
起点 SPEC-005: 出ていない所見 1 件（文書に属さない guard_liveness_gap）
起点 REQ-001 : 出ていない所見 0 件（同じ所見が行に出た）
```

上流の 5 検査は `doc_id` を空で挙げる。それらは**どの起点を開いても明細に出ない**
——この画面の問いの外に在る。一方、起点に繋がらない文書に付いた所見には
行き先が在る。**その文書の id である。**

`CHANGE-006` で「繋がらない 43 件」を三つに割ったのと同じ誤りが、所見の側に残っていた。

### 変えたもの

- **行き先が在る所見は、文書の id を押せる形で出す。** 押すとその文書が開き、
  開けば起点になる（新しい操作子を作らない）。
- **行き先が無い所見は「この画面の問いに属さない」と明示する。**
  「出していない」とだけ書くと、読み手は探せばどこかで出ると読む。
- 属するかどうかは `doc_id` と `refs` だけで判じる。**検査名を実装が持たない。**

## [0.5.0] — 2026-08-02

**画面に出た語の定義を、木の用語辞書から引いて出す。** 初学者の「帰結って何？」
「指紋って何？」に、画面がその場で答えるようになった（CHANGE-007・ADR-018）。

### 加えたもの

- **語の定義を脚注に出す。** 出す語は「画面に実際に出た文字列と辞書の承認語を
  突き合わせて」決める。**語の一覧を実装が持たない。**
  辞書の場所も `canonical_for: ['glossary']` から引くので、ファイル名も id も持たない。
- **辞書に 6 語を足した**（指紋・循環・所見・検査・統治木・範囲）。
  とくに「指紋」は画面に最も多く出る語なのに一行も無かった
  （辞書は「刻印」= 参照時点を定義しているが、指紋 = 内容の sha256 は別物である）。

### 直したもの

- **突然変異の門が、未追跡の新しいファイルで走行ごと落ちていた。**
  隔離木は commit から作るので未追跡のファイルが入らない。
  それまでの判定も読めなくなるので、表の行として不正と報せ、残りを走らせきる形にした。

### 実装で分かったこと

木の辞書には表が二つ在り、字面はどちらも三列である。自前で解いたら
**カルクの表まで承認語として拾った**（42 件。真は 33 件）。見分けるには見出しの語を
実装が持つことになり、単体試験がそれを捕まえた。

上流の `_termcheck.load_glossary` が既にこの表を解いている。承認語の判定を
上流へ返した。**規則を書き写すのではなく、規則を持つ側に訊く。**

## [0.4.0] — 2026-08-02

**画面が事実でないことを 17 箇所で言っていた。** 独立の走査が 47 件の疑いを挙げ、
反証段を通した 33 件を欠陥単位に畳んで 12 件。すべて自分で再現してから直した
（CHANGE-006・ADR-017）。

**そのあいだ、この製品の門はすべて緑だった。** 単体 174・突然変異 39・写し・統合・監査。

### 直したもの

- **強連結成分の取りこぼし。** `A↔B, B↔C` のとき `C` が画面のどこにも出ず、
  「起点から届かない 1 件」に化けていた。`C` は起点から届く。
- **件数が負になった。** 存在しない id と相互に指し合う辺があると `unreached = -1`。
- **起点が壊れていても「壊れている 0」と言い切っていた。** 起点は行にならず、
  所見が行にも要約にも脚注にも出なかった。起点の欄に記号と所見を出す。
- **「起点の外に所見 N 件」の「外」が「起点以外」だった。** 画面の行に全文が
  出ている所見も「外」に数えていた。**「画面のどこにも出ていない」**へ直した。
- **記号の排他で事実が数から消えていた。** `×` に負けた「足りない」が数に出なかった。
  記号ごと（和が文書数に一致）と事実ごと（互いに排他でない）を分けた。
- **辿る向きを言っていなかった。** 「繋がらない 43 件」のうち 11 件は
  **起点が前提にしているもの**で、「影響なし」ではなく「辿る向きが違う」。
  脚注を三行に分けた。
- **保存のたびに判定が丸ごと消えていた。** 脚注が「0 検査を走らせた時刻」という
  起きていないことを述べていた。`ADR-008` が禁じた事故の再来。
  引き継ぐ対象を「判定から導いた id」から**判定そのもの**へ広げた。
- **範囲が取れなかったことを「無い」と断定していた。** 取得の部分失敗で
  全部の行が `?`（直す場所が無い）に化けていた。
- **迂回路だけを名乗り、存在する直の辺を無いことにしていた。** 波は最長距離で
  決まるので直の辺を持つ文書が後の波へ回る。両方を言う形にした。
- **空白だけの題名が主文になっていた。** 画面で一番大きい文字が空白になっていた。
- **所見が `message` だけに潰れていた。** 六項をそのまま運ぶ。`severity` で色が付き、
  `path` が押せるようになった。

### 決めたこと（ADR-017）

- **画面は確かめたことだけを言う。** 知らないことを断定しない。性質の違う二つを
  一つの数に畳まない。記号の排他を数の規律にしない。判定から導いたものだけを引き継がない。
- **自分の門が緑であることを、正しさの証拠にしない。** 門は「作った者が想像した
  壊れ方」しか見ない。変更を閉じる前に、実装を書いた者とは別の走査を一度通す。
  これは `ECSS-E-ST-40C`（作る側）と `ECSS-Q-ST-80C`（点検する側）を別冊に分ける
  構造と同じである。
- **上流の値は素通しし、判断を足さない。**

### 門の側から出た欠陥

**突然変異の門が 47 行のうち 1 件を素通りさせた。** 最も重い欠陥（保存のたびに
判定が消える）で、実装は直っていたが**それを守る試験が無かった**。
受入を二つ足して 47/47 にし、潰しを実際に当てて赤 3 件を確かめた。

## [0.3.0] — 2026-08-02

**数の代わりに言葉を置くのをやめた。** `SPEC-006` が既に定めていた規律
「畳んだら必ず件数を書く」「良い状態を空白で表さない」を、自分で三箇所破っていた
（CHANGE-005・ADR-014）。

### 変えたもの

- **脚注が「全検査」と言い切るのをやめ、上流が走らせた数を数えて出す。**
  上流 `docs-audit.py --json` の `checks_run` を橋に通し、
  「上流の docs-audit（34 検査）を … に実行」と出す。**実装は数を持たない。**
  上流が増やせば脚注も試験の期待値も自動的に追随する。
- **行に status を出し、`superseded_by` が在れば後継の id を併記する。**
  実測で、非現行が現行と交互に混ざる（`DECIDED-001` を起点に置くと 17 行のうち
  4 件が非現行で波 3・4・4・5 に散る）。撤回して退役した文書が
  「直すことになるもの」の一覧に並んでいた。
  **彩度のある色は当てない。** 非現行は異常ではなく、ただの状態である。

### 直したもの

- **門が製品ではなく木の健康状態を測っていた。** 「上流の 34 検査すべてが橋を渡る」の
  確かめが `nonTrace.length > 0` で、追跡以外の所見が一件来れば通っていた。
  34 という数を検めておらず、**木が完全に緑になると橋が正しいまま門が赤くなる**
  状態だった。偽のプラグインが返す混在の報告で、木を一切読まずに検める形へ替えた。
- **`superseded_by` を取ったまま使っていなかった**（実装の消費者 0 件）。
- **写しの門が `undefined` を素通りさせた。** 写しの道具が `checksRun` を渡しておらず、
  脚注が `ran undefined checks` と出たまま「誤り 0」と報告した。
  **画面に `undefined` / `NaN` / `[object Object]` / `{0}` が出ないこと自体を門にした。**
  差し込みの失敗は型で守れない。出来上がった字面を見るしかない。
- **雛形が定める節から外れていた文書が三件。** うち一件は同じ日に書いた `ADR-014` 自身。
  `docs-audit` は error 0 / warn 0 / advisory 0 のままだった。

### 上流へ投げたもの

実測で裏づけて六本を書き直した。

- [#149](https://github.com/Forest-Project-Lab/doctrine/issues/149) `title` が `to_json` の白名簿から漏れている
- [#150](https://github.com/Forest-Project-Lab/doctrine/issues/150) `trace-index.py` が `.gitignore` を見ない
- [#151](https://github.com/Forest-Project-Lab/doctrine/issues/151) 同じ関係が二本の辺として返る
- [#152](https://github.com/Forest-Project-Lab/doctrine/issues/152) コア／支援／汎用を表せない。変わるのに追えない
- [#153](https://github.com/Forest-Project-Lab/doctrine/issues/153) 製品の目的を置く型が無い
- [#154](https://github.com/Forest-Project-Lab/doctrine/issues/154) 雛形の節を検める検査が無い

## [0.2.0] — 2026-07-29

**地図をやめ、明細にした。** 一つの画面が一つの問いにだけ答える形へ作り直している
（CHANGE-004・ADR-012）。答える問いは「これを変えたら、何を、どの順で直すことになるか」。

作り直した理由は、利用者からの一言である。

> 依存されているから何？　依存しあっているものは異常？など
> 逆に疑問が増えるばかりだった。

測ったところ、その読み方は正しかった。45 本の辺のうち 16 本が「同じ事実の二重書き」
（`A impacts B` と `B depends_on A`）で、地図はそれを向きの違う二本の矢印として描いていた。
**本当の循環は 0 件**である。画面が作った嘘を、利用者が正しく読んでいた。
加えて、上流の 34 検査のうち 11 件しか使っておらず、判断の層を橋の上で捨てていた。

### 変えたもの

- **地図・深度・レンズを捨てた**。段（L0–L3）も、色・絞り・配置・深度の四つのダイヤルも、
  保存したレンズも無い。旧版が「互いに独立な四つのダイヤル」と呼んでいたもののうち一つは、
  **数を四つにするためだけに足した選択肢**だった（SPEC-003 にそう書いてある）。
- **起点を利用者に選ばせない**。印が囲む範囲の中にカーソルが在ればその文書、
  統治木の `.md` を開いていればその文書が起点になる。カーソルが動けば明細が組み直る。
  組み直しは純粋な計算だけなので、上流を起こさない。
- **記号を五つに固定した**。重い順に排他で `× > + > ? > ! > ~`。
  それぞれがちょうど一つの上流の事実から出る。
- **`!`（直す）と `~`（見直す）を別の記号・別の一文にした**。
  上流が依存と影響を別の端として持ち、混ぜてはならないと定めているためである。
  方向を矢頭で運ばず、記号と文で運ぶ。
- **並びを最長距離にした**。`起点 → C → B` と `起点 → B` の両方があるとき、`B` は第 2 波である。
  最短で並べると第 1 波に出るが、`C` より先に `B` を直すと前提が二度変わる。
  循環（強連結成分）は一つの塊に畳むので、最長距離が全域で定まる。
- **循環に順を付けない**。波から外し、`A → B → A` という一行の文字列として書き下す。
- **上流の 34 検査すべてを橋に通した**。`check` が `trace` で始まるものだけに絞る形をやめた。
- **主文を題名にした**。`SPEC-003` という型番ではなく、その文書の題名が出る。
  上流はまだ `title` を返さないので、上流の frontmatter パーサをそのまま呼んで補う
  （[doctrine#149](https://github.com/Forest-Project-Lab/doctrine/issues/149)。届いたらこの層は捨てる）。
- **`DESIGN.md` を意匠の正本にした**（ADR-013）。寸法・色・書体・太さの正本は一つだけで、
  それは CSS ではない。試験が `DESIGN.md` を読み、そこに無い値が CSS に現れると落ちる。
  旧版は余白が六種類（うち四つが 4px 格子の外）、角丸が三種類、書体が八段
  （隣り合う比の中央値 1.02）、文字色の差が ΔE 2.23（見分けの付く閾値の下）だった。
- **`<svg>` を一要素も持たない**。座標も、辺も、矢頭も無い。

### 直したもの

- `docs-audit` の所見を追跡の検査だけに絞っていたのをやめた（23 件を捨てていた）。
- `_frontmatter.parse_file` の返り値を二つで受けていた（実際は三つ組）。
  全件が例外になり、握り潰して「題名の無い木」に化けていた。
  いまは**解析が全件で落ちたら失敗として返す**。黙って空を返さない。
- 意匠の門が、`DESIGN.md` の「`500` を使わない」という**禁止の文**から
  500 を許可値として拾っていた。許す値は表にだけ書く形へ直した。

### 上流へ投げたもの

- [#149](https://github.com/Forest-Project-Lab/doctrine/issues/149) `dep-graph.py --json` が `title` を返さない
- [#150](https://github.com/Forest-Project-Lab/doctrine/issues/150) `trace-index.py` が `.gitignore` を見ない
- [#151](https://github.com/Forest-Project-Lab/doctrine/issues/151) 同じ一つの関係が二本の辺として返る

## [0.1.0] — 2026-07-28

最初の版。公開前に、七〜八観点の監査と、その直しに対する反証検証を**六巡**ぶん回している
（所見 245 件 → 実在 209 件を反映）。各巡は前の巡の「直し」そのものを疑う形で回した。
毎巡、前の巡の直しから欠陥が出ている。四巡目では三巡目に入れた「画面が古びるのを止める門」が
一度も発火しないことが、六巡目では五巡目に置いた「直しが試験に守られていることを実測する道具」の
判定が自己成就していたことが分かり、そこから直している。

`tools/mutate-check.mjs` に並べた直しについては、`npm run mutate` が一つずつ潰して
対応する試験が落ちることを実測する。**並べていない直しについては何も言っていない。**
どの門も届かない層（状態の帯と余白の帯の繋ぎ）が残っていることは README に明記した。
統治の記録は `doctrine_docs/lens/decisions/` にある。

### 加えたもの

- **深度 L0–L3 の地図**。文脈の地図（ドメイン）→ ドメイン内部（文書）→ 文書（依存）→
  コード範囲。降りるのはダブルクリック、上がるのは `Backspace`。段ごとに操作を変えない。
- **レンズ**。色・絞り・配置・深度の四つのダイヤル。互いに独立で、名前を付けて保存できる。
  保存にはそのときの焦点も含めるので、選び直すと深度ごと同じ表示に戻る。
  保存先は作業フォルダごと。ダイヤルを回すと、当てていた組の名前は外れる。
- **文書 ↔ コードの往復**。印（`doctrine:begin`／`doctrine:end`）が囲む範囲の始まりに
  見出しを出し、押すと結ばれた文書が開く。文書からは結ばれた範囲へ跳ぶ。
  範囲の行には余白の帯を付ける。
- **指紋の食い違いの表示**。記録した指紋といまのコードが食い違っていれば、
  見出しと帯と地図の三つに出る。判定は上流の `docs-audit` に委ねる。
- **命令**: 地図を開く／取り直す、いま開いている文書を地図で示す、
  この範囲の文書を開く、この文書の実装へ跳ぶ、どの統治木を出すかを選ぶ。
  既定のキー割り当ては持たない（ADR-009）。割り当てたい利用者は `keybindings.json` に書く。
- **設定**: `pythonPath`・`pluginPath`（実行体を選ぶので machine scope）、
  `docsRoot`・`timeoutMs`・`autoRefresh`・`auditDebounceMs`。
- **信頼していない作業フォルダでは動かない**（`untrustedWorkspaces: false`）。

### 前提

- Python 3 と doctrine プラグインの二つ。実行時に Node は要らない
  （依存は配布物に束ねてある）。
- 統治木が無い作業フォルダでは、地図を出さずに敷き方を案内する。異常としては扱わない。
