---
id: IMPACT-006
title: 画面が確かめたことだけを言うようにすることの影響
type: IMPACT
domain: lens
status: current
owner: doctrine-lens-maintainer
created: 2026-08-02
updated: 2026-08-02
sources: []
depends_on: [CHANGE-006]
impacts: [SPEC-001, SPEC-004, SPEC-006, TEST-004, TEST-006, IMPL-001]
llm_context: task
---

# 画面が確かめたことだけを言うようにすることの影響

## 影響する文書

上流 `dep-graph.py --dependents ID --transitive` の実測にもとづく。

| 文書 | 何を直すか | 現行のままか |
|---|---|---|
| `SPEC-001` | 引き継ぐのは判定そのもの（所見・走らせた検査）であると書く。受入基準 10 を広げる | 現行のまま |
| `SPEC-004` | 所見は六項をそのまま呼び手へ渡す。範囲は取れなかったとき `null` で返す | 現行のまま |
| `SPEC-006` | 要約の形（記号ごと・事実ごと・循環）／脚注の三分割／起点の欄に記号と所見／行の一文が直の辺も言う／範囲が取れないときの記号 | 現行のまま |
| `TEST-004` | 範囲の部分失敗と真の不在を区別する確かめを足す | 現行のまま |
| `TEST-006` | 受入を七項足す（15・15b・16〜24 に対応） | 現行のまま |
| `IMPL-001` | 部品の表は変わらない（新しいファイルを作らないため）。`cadence.ts` の役の説明を直す | 現行のまま |
| `ADR-008` | **降ろさない。** 意図は正しく、覆う範囲が狭かっただけである。`ADR-015` が広げる | 現行のまま |

**降格する文書は無い。** 撤回する決定が無いので `WATCH` への追記も無い。

## 影響する実装

| ファイル | 何を変えたか | 実測の行数 |
|---|---|---|
| `src/model/consequence.ts` | 成分から件数を数える／件数を負にしない／起点の所見と記号／「外」の定義／`bySymbol` と `facts`／前提の到達／範囲の `null`／直の辺 | +118 |
| `src/model/cadence.ts` | `AuditCarry` に所見と検査を足し、三つを同時に動かす | +26 |
| `src/session.ts` | 引き継いだ判定で snapshot を詰め直す | +14 |
| `src/model/view.ts` | 要約の三行／脚注の三分割／起点の欄／所見の六項／空白の題名 | +52 |
| `src/shared/protocol.ts` | `FindingView`／`OriginView` の記号と所見 | +22 |
| `src/webview/main.ts` | 起点の記号と所見／所見の六項を描く | +34 |
| `src/panel/html.ts` | 起点の記号・所見の severity と押せる path | +20 |
| `src/panel/lensPanel.ts` | 範囲を `null` のまま渡す／題名の欠けを一件でも見る | +8 |
| `src/l10n.ts`・`l10n/bundle.l10n.ja.json` | 文言の差し替えと追加 | +22 |

**新しいファイルは作っていない。** だから `IMPL-001` の部品の表は変わらない。

## 影響するテスト

| 試験 | 何を変えたか |
|---|---|
| `src/test/consequence.test.ts` | 受入を 11 件追加（006-15〜006-24）。見本の文言を新しい型へ |
| `src/test/cadence.test.ts` | `AuditCarry` が三つ組になったので見本を直した |
| `tools/mutate-check.mjs` | **表を 39 行から 47 行へ。** 直した 8 件それぞれに潰しを一行 |
| `tools/shoot-preview.mjs` | 変更なし（前の変更で入れた「差し込みの跡」の門がそのまま効く） |

## 工数見積

| 段 | 内容 | 見積 | 実測 |
|---|---|---|---|
| 1 | 独立の走査を回し、挙がったものを再現する | 半日 | 半日 |
| 2 | 模型の欠陥 7 件 | 半日 | 半日 |
| 3 | 拍・波の文・範囲・所見・題名の 5 件 | 半日 | 半日 |
| 4 | 統治文書と正本の改訂 | 半日 | 半日 |
| 5 | 全門・指紋の再記録・PR | 半日 | — |

合計 二日半。

## 順序の不変条件

`DESIGN.md` と `SPEC-006` を実装より先に直す（`ADR-013`）。
今回は**実装を先に書いた**——独立の走査が挙げたものを再現する必要があり、
再現には実装を触るのが最短だったためである。**この順は例外であり、
正本を直さずに閉じてはならない。** この文書を書いている時点で正本の改訂が残っている。

`ADR-008` を先に読むこと。`ADR-015` は `ADR-008` を覆さず、覆う範囲を広げる。
